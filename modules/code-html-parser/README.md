# code-html-parser — HTML Code Parser

本解析器：解析 HTML/HTM，按标题/可见文本/script/style 切成统一 content-ir blocks。

切块规则：`切块规则.json`

## 对外能力

| 能力 | 说明 |
|------|------|
| `parse` | Parse HTML files into semantic content blocks |
