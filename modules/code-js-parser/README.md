# code-js-parser — JS/TS Code Parser

本解析器：解析 JS/TS/JSX/TSX，按 function / class / export 切成统一 content-ir blocks。

切块规则：`切块规则.json`

## 对外能力

| 能力 | 说明 |
|------|------|
| `parse` | Parse JS/TS files into semantic code blocks |
