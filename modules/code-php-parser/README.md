# code-php-parser — PHP Code Parser

本解析器：解析 PHP 代码文件，按 function / class / 文件头注释切成统一 content-ir blocks。

切块规则：`切块规则.json`

## 对外能力

| 能力 | 说明 |
|------|------|
| `parse` | Parse PHP files into semantic code blocks |
