# 节点⑨ 二次融合（deferred）

## 设计意图

同主体跨文档信息聚合为一个权威视图，并消解冲突。

## 为何 deferred

依赖⑦（干净主体名 + type_id）和⑧（碎节点归一 + 垃圾边砍掉）。未验证前激活会把脏数据融得更硬。

## 激活前置

1. ⑦ 小样：name 是主体词、type_id 非空率可接受
2. ⑧ 小样：华世王镞类碎节点可归并；相关边显著下降
3. 华哥拍板激活

## 接口

```python
from modules.knowledge.backend.services.节点09_二次融合 import 二次融合
await 二次融合(db, entity_id, owner_id)  # -> {"status":"deferred"}
```

当前 **未** 挂入 DAG。
