# 节点⑩ 增量回填

## 设计意图

新文档入库后，经共享实体牵连旧文档，刷新主体聚合视图，并防 A↔B 死循环。

## 接口

```python
from modules.knowledge.backend.services.节点10_增量回填 import 回填
await 回填(db, new_document_id, owner_id, 提交=True, max_depth=1)
```

## 落盘

- 视图：`kb_entity_subject_views`（调用 ⑨）
- 账本：`kb_backfill_ledger`
- **不改** `kb_raw_data` / `kb_page_fusions.fused_text`

## 边界

- **未挂 DAG**
- 不碰 pipeline_service
