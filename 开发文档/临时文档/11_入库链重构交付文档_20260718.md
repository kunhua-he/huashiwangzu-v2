# 入库链模块化重构 · 交付文档 2026-07-18

> 新会话入口。直接读本文，5分钟上手当前状态，不用扫代码。

---

## 一、背景一句话

知识库语料一直脏、实体碎、图谱不可遍历。根因是入库链算法问题，不是算力问题。
本次按华哥10节点架构做了全链路模块化重构。

---

## 二、今天完成的节点（验收通过）

| 节点 | 路径 | 核心内容 |
|---|---|---|
| ②A 新增解析器 | `modules/doc-parser/` `modules/code-*-parser/` | doc从docx拆出；py/php/html/js/java/go/c/cpp等代码文件可入库；切块规则外置json；`backend/app/services/content/package_service.py` 和 `knowledge/parsing_service.py` 两份映射表同步 |
| ③ 三路纠错回写 | `services/节点03_三路交叉纠错/` | 文本层当尺子纠OCR/VLM错字；护栏已收紧（权威最少1→2/否定词evidence<3/品牌字黑名单/空上下文驳回）；5036行旧污染已回滚；与graph.py接线 |
| ④ 图像描述OCR分家 | `services/节点04_图像描述/` | VLM描述与OCR认字分成独立接口；raw_vision提示词去掉"可见文字"重叠，聚焦纯视觉 |
| ⑥ 单页融合改造 | `services/节点06_单页融合/` | 有文本层(round1>50字)→直接落盘不烧LLM（节省94.4%）；图片页走grok-4.5兜底；与fusion.py接线 |
| ⑦ 文档级串联 | `services/节点07_文档级串联/` | 实体抽取+分类落type_id+同文档归并；提示词精修（只取主体剥修饰语+18类合一）；与graph.py接线 |
| ⑧ 跨文档串联 | `services/节点08_跨文档串联/` | 跨文档实体归并；相似边门禁（阈值从0.15提高+主体实体共现条件）；主体关系建边；与relations.py接线 |
| ⑨⑩ 骨架 | `services/节点09_二次融合/` `services/节点10_增量回填/` | 骨架已建，返回`{"status":"deferred"}`，子文件已占位，**算法未实现** |

---

## 三、精度修复（我直接改的，不是grok）

### 3.1 models.json 变更
文件：`backend/data/config/models.json`

- 新增 `grok-4.5-vision` profile（grok-local-gateway，grok-4.5，VLM专用）
- 新增 `gpt-5.5-local` profile（gptstore-local，gpt-5.5，entity抽取专用）
- `stages.raw_ocr + raw_vision` → `grok-4.5-vision`（原gpt-5.5-vision）
- `stages.entity` → `gpt-5.5-local`（原deepseek-v4-flash）
- `pipeline_concurrency.entity_extract` → 200
- `gptstore-local` provider：api_url改为 `http://localhost:50936/v1/chat/completions`，api_protocol改为 `chat`（原来是responses协议，50936只支持chat）

### 3.2 数据库提示词变更

| id | name | 变更 |
|---|---|---|
| 2 | knowledge_entity_extraction | 全新重写：只取主体剥修饰语+18类合一+confidence字段+9种关系谓词 |
| 6 | knowledge_raw_vision | 去掉第1条"可见文字"（和OCR重叠），聚焦纯视觉描述 |

### 3.3 当前LLM/VLM状态：**全部暂停**

pipeline_concurrency全部设为0（entity_extract/graph/raw_vision/raw_ocr/fusion/page_fusion），防止继续消耗额度。

---

## 四、全库重跑状态（关键！）

### 已完成
- 下游数据已全部清空：
  - `kb_entity_dictionary` 删107868条
  - `kb_graph_nodes` 删105706条
  - `kb_graph_edges` 删169916条
  - `kb_chunk_entities` 删1804180条
- `kb_documents.graph_status` 全部重置为`pending`（15620个文档）
- **15251个文档的graph阶段任务已入队**

### 卡点（一个动作解决）

**后端没有重启，models.json配置改动未生效。**

worker在消费任务时，gptstore-local还用旧的responses协议→ 50936返回401。

### 新会话第一件事

1. **重启后端**（让models.json chat协议生效）
2. **把LLM/VLM并发恢复**（改models.json）：
   ```python
   # 恢复并发
   entity_extract = 200
   graph = 200  
   # 其他视情况
   ```
3. worker自动消费15251个任务 → ③纠错（纯规则）+ ⑦实体抽取（50936/gpt-5.5）自动跑完

---

## 五、未完成的纯代码任务（不需要LLM/VLM，可立即做）

这四个是**纯代码**，不烧额度，应该立刻发一封大信给grok实现。

### ②B 规则热加载

- 现状：代码解析器的切块规则已外置到 `modules/code-*/切块规则.json`，解析器读json执行
- 缺的：改规则还需要重启后端（没有热加载机制）
- 要做：监听json文件变化，或加一个reload接口，改规则不需要重启
- 文件边界：只改各code-*-parser的parser.py和路由，加reload capability

### ⑤ 文本汇流

- 现状：raw_collection_service.py把三轮数据分别存kb_raw_data，没有统一的"按文档按页汇总"接口
- 要做：新建`节点05_文本汇流/`，单接口`汇流(db, document_id, owner_id, page) → {text, ocr, vision, vision_desc}`
- 复用：读kb_raw_data按document_id/page/round返回三路内容
- 下游：⑥融合和③纠错都需要三路数据，现在是各自查DB，汇流层统一之后更清晰

### ⑨ 二次融合（填充骨架算法）

- 骨架位置：`services/节点09_二次融合/` （主体聚合.py / 冲突消解.py / 对外接口.py）
- 要实现：
  - `主体聚合.py`：按entity_id查kb_page_fusions里提到该实体的所有页，聚合成一个主体视图（不烧LLM，纯DB聚合）
  - `冲突消解.py`：检测同主体跨文档矛盾信息（如A说5品牌B说6品牌），写入conflict_json字段，不自动解决，标记等人工
  - `对外接口.py`：把status从deferred改为实际调用

### ⑩ 增量回填（填充骨架算法）

- 骨架位置：`services/节点10_增量回填/`（关联文档发现.py / 信息回填.py / 防循环幂等.py / 对外接口.py）
- 要实现：
  - `关联文档发现.py`：新文档入库后，通过共享实体（kb_chunk_entities）找到相关旧文档
  - `信息回填.py`：把新文档里的信息更新到旧文档的聚合视图（新增字段/覆盖字段）
  - `防循环幂等.py`：记录已处理的(新doc,旧doc)对，防止A→B→A死循环；用kb_entity_merge_log或单独一张表
  - `对外接口.py`：把status从deferred改为实际调用，接入pipeline_service的successors（relations之后触发）

---

## 六、关键表结构（给grok参考）

```
kb_raw_data: document_id/page/round(1=text,2=ocr,3=vision)/source_type/content/metadata_json/status
kb_page_fusions: document_id/page/fused_text/fusion_status
kb_entity_dictionary: id/owner_id/name/type_id/category/status/canonical_id
kb_semantic_types: id/type_name/definition（18条）
kb_graph_nodes: id/owner_id/entity_id/label/category
kb_graph_edges: source_node_id/target_node_id/relation/weight
kb_chunk_entities: document_id/entity_id/chunk_id
```

---

## 七、环境

- Python：`backend/.venv/bin/python`（注意不是.venv，是backend/.venv）
- DB：PostgreSQL 华世王镞_v2，127.0.0.1:5432，postgres
- owner_id恒为4
- grok：http://127.0.0.1:8317/v1，Bearer 123，grok-4.5
- 本地号池：http://localhost:50936/v1，Bearer agt_codex_ObOla6ZsThNlW1KdD7Ykmj5ZmH6FFkO1，gpt-5.5（chat协议）

---

## 八、下一封大信建议

把②B/⑤/⑨/⑩打包一封信给grok，纯代码，不需要额度，立即可做。
信件边界：只新建/填充上述模块，不碰已完成的③④⑥⑦⑧，不碰pipeline_service，不批量重跑，不推远程。
