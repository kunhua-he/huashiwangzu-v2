---
name: "R5 第一阶段主会话集成验收与小批生产治理"
type: "task"
tags: [r5, conductor, knowledge, dev-toolkit, release-gate, production-apply]
agent: "codex-conductor-r5"
created: "2026-07-03T15:51:35.826082+00:00"
---

# 做了什么

主会话恢复旧线程 `019f268c-0710-7dc2-be55-538dc2e8c43b` 后，接手 R5 第一阶段五泳道结果并完成集成验收：

- A knowledge source-unavailable 状态机：源文件 missing/deleted 时统一 `source_unavailable`，`search_ready/deep_ready=false`，历史 all-done 不再假 ready。
- B knowledge guarded archive：只允许 `doc_missing/source_file_missing/source_file_deleted` 转 completed skipped，保留 previous error/result，并同步 source-unavailable parse_error。
- C knowledge orphan running reconcile：新增 `reconcile_orphan_pipeline_runs` capability 与 HTTP governance 端点，收口 `kb_pipeline_runs.status='running' AND task_id IS NULL` 的诊断行。
- D dev_toolkit baseline 归因：`worktree_guard/finish_task` 支持 baseline paths/status JSON，能区分开工前 dirty 与本 agent 新增越界。
- E dev_toolkit response trim：`probe/call_capability` 支持 `selector/json_path/max_items/max_bytes`，大响应可按字段裁剪且默认兼容旧输出。

# 生产治理

- 对 knowledge 历史 `kb_pipeline` failed 队列执行精确小批 apply：三类各 5 条，共 15 条，`changed=15/skipped=0`。
- 对 orphan running 执行 apply：run_id 8/31/33/53，共 4 条，`changed=4/skipped=0`，复核后 orphan running dry-run `matched=0`。
- 计数变化：knowledge `kb_pipeline failed 769 -> 754`；全局 task queue failed `905 -> 890`。

# 验证

- CodeGraph 查过 router/dev_toolkit 影响面。
- ruff 覆盖 15 个改动 Python 文件，全通过。
- focused tests 合跑 75 passed：`dev_toolkit/test_worktree_tools.py`、`test_response_tools.py`、`test_server_helpers.py`、knowledge ingest/debt/reconcile 三组测试。
- 后端重启后 `/api/health` ok，knowledge capability/route 运行态可见。
- `release_gate.py --skip-ui`：`PASS_WITH_DEBT`，无 BLOCKER；sandbox matrix 35 modules / 35 pass / 0 skip；gate-run failed delta 未新增；orphan running PASS。

# 残留风险

- 历史 failed debt 仍有 890，这是存量债，非本批新增。
- UI gate 仍 skip，保持 DEBT。
- knowledge 分类窗口仍有大量 `source_file_missing/source_file_deleted/doc_missing`，后续可继续 guarded batch；`greenlet_spawn/parser_no_content/duplicate parse lock/file_row_live retry` 应进入第二阶段专项。

# 关联 commit

- `90fe0dc0 complete r5 first-stage knowledge and mcp fixes`
