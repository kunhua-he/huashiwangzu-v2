---
name: "R6 第二阶段五线程边界与硬指标"
type: "decision"
tags: [r6, conductor, second-stage, knowledge, media, release, mcp]
agent: "codex-conductor-r6"
created: "2026-07-03T15:59:11.237335+00:00"
---

# R6 背景

用户指出 R5 成效偏小。主会话确认：R5 是第一次真实写库的小批安全验证，代码轨道较大但生产债务只下降 15+4。R6 改为每条线带硬指标，不只做纸面修复。

# 当前基线

- 分支：`codex/r6-second-stage`，从 `2e2994f5 merge r5 first-stage fixes` 开始。
- 工作区：开工干净。
- 全局 task queue：failed=890, pending=1, running=0。
- knowledge classify_pipeline_debt(limit=500)：async_context_error=4, parser_no_content_blocks=2, doc_missing=19, source_file_missing=303, file_row_live=13, source_file_deleted=158, duplicate_or_stale_parse_lock=1。

# 五线程

A `codex-r6-knowledge-batch-a`：knowledge 生命周期债大批治理能力。目标让主会话可按 category/limit_each 等参数一次安全 apply 至少 60 条，真实 apply 由主会话执行。

B `codex-r6-knowledge-defects-b`：knowledge greenlet/parser/lock/retry 代码缺陷专项。目标至少让一个代码缺陷 family 变为可安全 retry 或不再新增。

C `codex-r6-media-local-facts-c`：media-intelligence 本地事实层。目标 happy path 不再输出 local_algorithms.placeholder，改为真实 metadata/degraded reason。

D `codex-r6-release-ui-d`：release clean pass / UI gate。目标至少真实跑一次 UI gate 或 Playwright，形成可修红项或修通测试基建。

E `codex-r6-mcp-conductor-e`：MCP conductor/批量状态。目标交付一个主会话能直接用的多代理状态/提交整理工具，减少人工追踪。

# 主会话策略

- 不抢 worker 写范围；只做只读控盘、验收和集成。
- 任一 worker 返回后先复核边界，再 focused lint/test，再按切片提交。
- 生产 DB apply 必须由主会话在 dry-run changed/skipped 符合门禁后执行。
- 这一批验收不能只报测试绿，必须报告至少一个可量化结果：failed debt 下降、placeholder 消失、UI gate 红项明确、或 conductor 工具可用。
