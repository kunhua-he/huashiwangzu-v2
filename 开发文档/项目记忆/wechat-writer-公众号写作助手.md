# 项目记忆：公众号写作助手（wechat-writer）

- **Agent**：执行者（2026-06-26）
- **做了什么**：从 0 搭建 `modules/wechat-writer/`，实现选题→大纲→成文→成分校验→草稿管理的公众号写作一条龙。后端 12 个端点 + 4 个跨模块能力，前端 4 个 Vue 组件。接入模型网关 deepseek-v4-flash 和知识库 search。
- **关键决策**：提示词存 DB 动态加载（不是硬编码）；成分校验双轨制（知识库 + AI）；owner_id 隔离。
- **真生成了**：一篇「脸又开始闹情绪？换季红痒刺痛，其实是屏障在求救！」的完整初稿（2531 字），已存草稿。
- **关联文件**：`modules/wechat-writer/` 下全部文件
- **构建验证**：vue-tsc 通过，scan-modules 注册成功。全量构建因 `im` 模块的 element-plus 问题失败（不阻模块独立运行）
- **待深化**：配图推荐、文章排版、异步生成、知识库内容建设、历史文章风格学习、公众号 API 发布
