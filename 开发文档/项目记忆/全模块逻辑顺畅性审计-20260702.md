---
name: "全模块逻辑顺畅性审计-20260702"
type: "task"
tags: [audit, modules, security, contracts, subagents]
agent: "codex-conductor"
created: "2026-07-02T11:03:38.601137+00:00"
---

只读审计 modules 全局逻辑顺畅性，协调 4 个子代理并总控复核。活栈 health/system-status 绿，release_gate --skip-ui 为 PASS_WITH_DEBT；module_sandbox_matrix 34 modules / 28 pass / 0 fail / 6 skip。关键发现：P0 docs-open 对共享只读文件可经 write_content 以 owner 身份 replace；docs-open token 三件套 content 接口未执行 doc scope；P0 excel-engine state_key 仅按 knowledge_{file_id} 全局唯一且 db_ops 不按 owner 过滤；P1 im:send capability 缺成员校验；browser-tools SSRF 仅字符串拦截且 session 不绑定 caller/多 worker 不稳；scheduler 与 agent 后台通知使用未白名单 caller；agent subagent_runner skill_describe owner_id 未定义；doc/ppt viewer 读取旧 content shape 而 parser 返回 blocks/resources；web-tools 后台模块误声明 component_key；wechat-writer 前端裸 fetch/token 与大量 any；中文开发记录文件在 modules 下。未发现 public_actions/register_capability 名称级不一致；未发现 parser 直接绕过 check_file_access。
