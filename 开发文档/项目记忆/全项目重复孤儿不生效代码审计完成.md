---
name: "全项目重复孤儿不生效代码审计完成"
type: task
tags: ["审计", "重复逻辑", "孤儿代码", "不生效代码", "冗余路径"]
created: 2026-06-30
agent: opencode
---

## 审计结论

完成全项目 8 类专项审计，输出报告到 `开发文档/项目记忆/全项目重复孤儿不生效代码审计报告.md`。

### 核心发现（按优先级）

**P0:**
1. `/api/office/status/{file_id}` 返回 500（check_file_access 签名不匹配）
2. `framework_file_json_*` 四张旧表 0 记录但 ORM 模型仍在 import
3. knowledge ingest 与 content:pipeline 两套独立 parse 路径
4. check_file_access 函数在 file_service 与 file_share_service 中返回类型不一致

**P1:**
1. 三个导出入口（content:export、office-gen:generate_to_artifact、artifact:export）行为不一致
2. 7 个 parser 模块 + knowledge 内部 parse = 8 套 parse 实现，无 canonical
3. content 能力未注册为模块 capability，Agent 工具发现无法枚举
4. excel_versions 表 0 记录，版本管理不生效
5. 15+ 模块 runtime/index.ts 各重复 150+ 行 boilerplate

**P2:**
1. `/api/office/patch/*` 和 rollback 端点返回 "deprecated" 空壳
2. 旧 office service 残留文件
3. 旧文档未更新

### 维修批次建议
- 批 A: P0 主链路与权限修复
- 批 B: 重复业务线收口（指定 content package 为 canonical）
- 批 C: 孤儿代码删除 + 文档同步
- 批 D: runtime 去重 + Agent 工具指引补齐

### 注意事项
- 当前 Git 工作分支（codex/repair-agent-foundation-09-r1）已有 43 个修改文件，部分发现可能已修复但未提交
- 审计过程通过 `office-gen:generate_to_artifact` 产生了测试数据（artifact_id=13, file_id=668），需清理
