---
name: "全项目重复孤儿不生效代码审计报告核验"
type: task
tags: ["audit", "content-package", "office", "repair-letter", "opencode"]
created: 2026-06-30
agent: codex
---

核验 opencode 输出的《全项目重复孤儿不生效代码审计报告》。确认：/api/office/status/{file_id} 活栈 500，根因是 office.py 将 file_service.check_file_access 返回的 File ORM 当 dict 读；framework_file_json_* 四张旧表全 0 且旧 ORM/service 仍残留；/api/office/patch/* 与 /api/office/rollback 返回 deprecated 假成功；excel_versions 为 0 且版本写入路径很窄；runtime/index.ts 重复属真实 P1。纠正：content 能力通过真实 /api/modules/capabilities 已注册可发现，MCP capabilities(module=content) 为空只是工具台 manifest 扫描局限；knowledge ingest 已先触发/读取 Content Package，parse fallback 是兼容路径，不应按 P0 双主链路处理；office-gen 当前磁盘文件已调用 ContentPipelineService.run_pipeline_for_package，不应退回。已产出维修信建议 opencode 修复 P0 主链路与旧空壳收口。
