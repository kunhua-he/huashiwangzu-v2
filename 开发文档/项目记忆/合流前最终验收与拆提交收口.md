---
name: "合流前最终验收与拆提交收口"
type: "task"
tags: [acceptance, release-gate, modules, post-convergence, sandbox]
agent: "codex-final-acceptance"
created: "2026-07-03T18:43:26.342139+00:00"
---

# 改了什么

本轮按附件要求执行合流前最终验收/收口，不主动扩大修复范围，不主动提交。验收过程中并行流程创建了提交 19cd4101（fix: converge tooling and module hardening repairs），工作区从 56 个混合 dirty 收敛为少量剩余 dirty；因此 release gate 结论以当前 HEAD 复跑结果为准。

# 验证了什么

- 工作区：验收前为 post-convergence framework/tooling/UI、modules、开发文档/项目记忆混合 dirty；worktree_guard 当时 changed_count=56，允许范围内 0 forbidden。并行提交后当前剩余 dirty 主要为 backend/pytest.ini、dev_toolkit/README.md、dev_toolkit/test_module_sandbox_matrix.py，以及本轮记忆/反馈文件。
- capability contract：MCP capability_contract_diff(include_parameters=true) 为 36 checked / 29 ok / 0 drift / 0 uncheckable；scripts/check-capability-drift.py 输出 OK (172 registered public capabilities)。
- module sandbox：backend/.venv/bin/python dev_toolkit/module_sandbox_matrix.py --check --json 真实执行，summary total=35 / pass=35 / fail=0 / skip=0。
- parser/media 文件访问静态审计：12 个目标目录均存在；rg 未发现裸 db.get(File) 或 select(File)；storage_path 仅 runtime 类型定义命中；后端统一入口命中 run_uploaded_file_capability -> read_uploaded_file -> check_file_access。
- lint/test：指定变更 Python 文件 ruff All checks passed；backend reset 19 passed；dev_toolkit 定向 29 passed, 1 skipped, 2 warnings；node --check frontend/tests/ui-e2e.spec.mjs 通过；Playwright 5.2 + cleanup 2 passed；dev_toolkit/test_smoke_queue_gate.py 5 passed。
- release gate：并行提交前曾连续两次 BLOCKER（缺 summary.failed baseline）；当前 HEAD 复跑 backend/.venv/bin/python dev_toolkit/release_gate.py --skip-ui --preflight 已通过为 PASS_WITH_DEBT，release_safe=true，clean_pass=false，0 BLOCKER，3 DEBT（skip-ui、preflight 未跑 smoke、preflight 未跑 sandbox matrix）。

# 是否还有残留风险

当前可按已有提交 19cd4101 作为大收敛提交看待，但若继续提交剩余 dirty，仍建议先拆分归因。browser-tools session 进程内状态按后续独立任务处理，不混入本轮。日志尾部有 UI cleanup 触发的 knowledge parse deleted-file error/warning，测试通过但不应视为 clean release。
