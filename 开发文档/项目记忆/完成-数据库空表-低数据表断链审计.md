---
name: "完成：数据库空表/低数据表断链审计"
type: "task"
tags: [audit, database, empty-table, backtrace, db-empty-backtrace-r7]
agent: "db-empty-backtrace-r7"
created: "2026-07-02T18:08:50.503483+00:00"
---

db-empty-backtrace-r7 完成只读审计。读取了 开发文档/README.md、底层/模块/agent/knowledge/memory README；使用项目工具台 brief/plan_task/worktree_guard/db_schema/code_explore/code_node/tail_log/sql。未修改业务代码，仅按要求写入项目记忆。

关键结论：1) `framework_system_tasks=0` 不是后台任务断链，当前任务权威表是 `framework_system_task_queues=2233`，旧 personal task 表无写入点。2) knowledge 精确溯源层疑似断链：`kb_page_fusions=799`、`kb_evidence=316`、`kb_entity_dictionary=35`，但 `kb_chunk_entities=0` 且 `kb_evidence.chunk_id=0` 为 316/316；代码中 entity_service 明确写死 `chunk_id=0`，未创建 KbChunkEntity。3) memory 语义链路偏弱：`memory_records=37` 但仅 4 条有 embedding，`memory_chunks=6`，`memory_links=0`；dream 建链需要 embedding，当前无法形成链式召回。4) experience learning 未沉淀：`memory_experiences=0`，Agent 仅在回复含“最佳路径总结”时保存经验，触发过窄。5) imagegen_records=0 与 README 历史“实测可用”存在冲突，可能是库切换/清理或生图未在当前库执行。6) agent_understanding_*、agent_skill_usage、agent_tool_guide_versions 为空，多数是条件触发/治理功能未使用；其中 usage 为空说明技能治理统计没有覆盖当前外部 skill 使用通路。

验证：只读 SQL 精确 count、任务队列分布、knowledge/memory 关键统计；CodeGraph/rg 定位写入点；未执行写入型 probe。关联 commit：无，本任务只审计不改代码。
