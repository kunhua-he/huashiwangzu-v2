# 审计修复03 — 后端性能与并发健壮

**Agent**: executor  
**时间**: 2026-06-25T23:45  
**Commit**: (未提交)

## 干了啥

1. **S5** — `modules/knowledge/backend/services/search_service.py` 的 `vector_search()` 改用 pgvector `ORDER BY embedding <=> CAST(:emb AS vector(1024)) LIMIT :k`，不再全表加载到 Python。列类型 JSON→vector(1024)。加 HNSW 索引。
2. **S9** — 15 个缺失索引通过 migration 创建（`framework_file_items/folders/shares/json_versions/json_patches`、`agent_messages`、`im_messages`）。
3. **D3** — `backend/app/services/circuit_breaker.py` 从进程内 dict 改为 DB 持久化（`framework_circuit_breaker_states` 表），`SELECT FOR UPDATE` 原子状态变更，跨 worker 可见。

## 踩过的坑

1. `EXTRACT(EPOCH FROM ...)` 返回 `decimal.Decimal` 与 `time.time()` 的 `float` 运算报错 → 加 `float()` 转换。
2. `postgresql.VECTOR` 不存在 → 需用 `pgvector.sqlalchemy.Vector`。
3. `alter_column` 不支持 `USING` 子句 → 改用 `op.execute("ALTER TABLE ... ALTER COLUMN ... TYPE vector(1024) USING ...")`。

## 遗留问题

- S5 结果一致性需有数据后才能真实验证（当前 0 chunk）。
- `_test:always_fail` 测试残留一条 OPEN 记录。
