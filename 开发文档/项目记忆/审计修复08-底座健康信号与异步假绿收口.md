---
name: "审计修复08-底座健康信号与异步假绿收口"
type: task
tags: ["审计", "健康检查", "异步队列", "Excel", "假绿"]
created: 2026-07-02
agent: opencode
---

完成三处假绿风险收口：(1)统一 worker 健康信号——system_status_service 改用 task_worker.worker_health() 替代 psutil 进程名扫描, /api/system/status 与 /api/health 的 worker 状态一致 running=true; (2)异步队列验收——smoke.py 改为延后删除文件+轮询等待队列静默, 增加 Z1/Z2 验收命令验证 smoke 后 failed 不新增、pending 不超过 5; (3)Excel fake-success——router.py 加 _legacy_result_to_api 转换函数, 所有端点中 result.code!=0 时正确返回 success:false + error, 不藏失败在 data.code 里。TypeScript 债务扫描: frontend/src 仅 1 处 as any(全局 window 注入)。
