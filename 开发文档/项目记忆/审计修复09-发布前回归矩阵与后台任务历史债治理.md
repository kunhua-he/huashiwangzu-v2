---
name: "审计修复09-发布前回归矩阵与后台任务历史债治理"
type: task
tags: ["审计修复09", "release-gate", "task-queue-audit", "sandbox-matrix", "框架底座"]
created: 2026-07-02
agent: opencode
---

# 改了什么

三条线并行：
- **A线 任务队列历史债**: 新增 `task_queue_audit_service.py` — 分类历史债 vs 新增失败，reconcile 孤儿任务；`GET /api/tasks/worker/audit` 诊断端点（per-handler、错误签名、stalest pending）；`POST /api/tasks/worker/reconcile` 管理端点
- **B线 模块 sandbox 矩阵**: `module_sandbox_matrix.py` — 扫描 34 模块，8 可自动验收、26 跳过（有明确原因），支持 `--check`/`--json`
- **C线 release gate**: `release_gate.py` — 聚合 health/system-status/smoke/队列审计/sandbox 矩阵，输出 BLOCKER/DEBT/PASS；集成到 MCP server

# 验证了什么

- smoke 28/28 全绿
- 23 pytest + 2 脚本测试全绿
- 7 lint 文件全通过
- release gate 产出 BLOCKER（7 个新增失败来自测试历史债，正确分类；非阻塞类记为 DEBT）
- sandbox 矩阵产出 34 模块 8 pass 0 fail 26 skip

# 是否还有残留风险

- 26/34 模块缺口已记录矩阵但未补齐测试
- 7 个 24h 内新增失败来自前置任务（不是本次引入）
- pending=1 future_scheduled 正常（2026-12-31 调度任务）

# 关联 commit

尚未提交
