---
name: "开放性审计 — 华世王镞 V2 当前分支质量、架构、功能与边界复核"
type: task
tags: ["审计", "架构", "安全", "check_file_access", "API契约", "media-asr", "多worker"]
created: 2026-06-30
agent: opencode
---

# 开放性审计报告

## 执行 Agent
opencode

## 审计范围
- A 架构边界: 框架/modules 边界、跨模块 import、media-asr 审查
- B 文件权限: check_file_access 全链路追踪
- D 多 worker: SKIP LOCKED、文件锁持久化
- E API 契约: success=False 违规扫描
- F 前端类型: as any/ts-ignore 清零验证
- G 测试: pytest 452 passed
- H 依赖: mlx_whisper/no_proxy/ffmpeg

## 关键发现
### P1 (需安排修复)
1. **API 契约违规**: modules/browser-tools/router.py:100 + modules/agent/router.py:162/183/517 共 4 处 `return ApiResponse(success=False)` 返回 200 → 应改为 `raise ValidationError`
2. **media-asr 模块结构缺失**: 缺 runtime/、sandbox/、tests/ 标准目录
3. **media-asr component_key 不可展示**: component_key 非空但所有展示开关为 false

### P2 (建议处理)
1. **任务失败积压 683 条**
2. **no_proxy 环境变量全局突变** (audio_service.py:107-121)
3. **Apple Silicon 硬依赖无备选**

### P3 (后续清理)
1. github-search @router.on_event 弃用
2. test_agent_e2e_regression fixture 弃用模式

## 已确认无问题
- check_file_access 覆盖全部文件读取路径
- 无跨模块 import/直接读表
- manifest vs register_capability 三能力对齐
- 框架 router 无 success=False 违规 (0 处)
- frontend/src 无 as any/@ts-ignore (0 处)
- task_worker 用 FOR UPDATE SKIP LOCKED 防竞态
- codemap 锁持久化为 locks.json 文件
- 452 测试全绿

## 总体结论
无 P0 阻断，可合入。建议在后续维修批次覆盖 P1 项。
