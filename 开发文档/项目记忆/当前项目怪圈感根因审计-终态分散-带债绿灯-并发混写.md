---
name: "当前项目怪圈感根因审计：终态分散 + 带债绿灯 + 并发混写"
type: "gotcha"
tags: [audit, r6, conductor, knowledge, release-gate, media-intelligence]
agent: "codex-project-audit-current-state"
created: "2026-07-03T16:19:31.343584+00:00"
---

2026-07-04 只读审计结论：项目不是单点坏，而是多套状态源没有唯一终态。agent_board 显示多数任务 completed，但 git dirty、投递箱、项目记忆、MCP feedback、OpenCode job、release gate 与真实 UI 测试仍各自保留未收口信号，导致下一轮 agent 继续误判“还有活”。活系统 /api/health 为 ok，但 framework_system_task_queues 仍有约 892-893 failed，其中 kb_pipeline 约 756/757、profile_evolve 约 135；release_gate(skip_ui=true) 为 PASS_WITH_DEBT，不是 clean pass。knowledge 的治理工具 apply_pipeline_debt dry-run 可一次选中 60 条 changed=60 skipped=0，但尚未生产 apply，所以债务未下降。media-intelligence 当前只是媒体分析契约 + 本地事实 MVP，OCR/object/small_model/VLM 多为 degraded，不应按完整媒体智能验收。边界未发现 P0 跨模块 import/越权读盘，但共享工作区多任务混写、超大文件回潮、中文文件名在 modules 下、sandbox dist 入仓、少量 any 仍需治理。建议止血：冻结新派发；先收束 codex/r6-second-stage，按 dev_toolkit/frontend-tests/knowledge/media-intelligence/项目记忆分组验收提交或退回；真实 apply 生命周期债务 60 条前后验队列；修 UI delete+recycle；把 release gate 默认改为包含 UI；归档已交付投递箱；以后多代理用独立 worktree 或每泳道分支，并让 complete 绑定 commit/verification/dirty_scope。
