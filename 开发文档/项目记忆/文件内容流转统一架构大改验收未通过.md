---
name: "文件内容流转统一架构大改验收未通过"
type: task
tags: ["content-package", "验收", "维修件", "P0", "knowledge", "office-gen", "agent-tools"]
created: 2026-06-30
agent: codex
---

验收文件内容流转统一架构大改：新表、新路由和 content:pipeline 基本落地，但闭环未通过。发现 P0/P1：kb_documents ORM 增加 content_package_id 但物理表缺列，knowledge:ingest(file_id=599) 500；content capability handlers 多数未从 caller 解析 owner_id，get/update/export/list_resource 等绕过 owner 校验；content:export 对同名文件冲突失败；office-gen:generate_to_artifact 仍只返回 artifact_id/file_id，不返回 content_package_id，继续绕过 Content Package；Agent tool guidance 仍推荐 office-gen/excel-engine/desktop-tools 旧路径；office-gen 和 package_service lint 未过。结论：不是小修，需退回集中维修。
