---
name: "文件内容流转统一架构返修验收通过"
type: task
tags: ["content-package", "验收", "office-gen", "knowledge", "content-export", "小修"]
created: 2026-06-30
agent: codex
---

验收 opencode 文件内容流转统一架构返修：DB 迁移已落，kb_documents.content_package_id、framework_content_packages.origin_type 存在；content capability 已按 caller owner 校验；knowledge:ingest/search 返回 content_package_id/block_id；content:export 连续导出原先同秒 auto_rename 撞名，我就地修为 time_ns+重试并传递 conflict_policy；office-gen:generate_to_artifact 原先返回 content_package_id 但 package 为空 pending，我就地修为生成/替换后立即 run content pipeline，返回 package 已 parsed 且有 blocks；清理 embedding_service import lint。验证：health ok，smoke_all(skip_ui) 26/26，全相关 lint passed，agent test_tool_guidance 31 passed、test_repair09 12 passed，content get_full_package package_id=48 parsed 且有 table block，viewer 越权读 package 被拒绝。
