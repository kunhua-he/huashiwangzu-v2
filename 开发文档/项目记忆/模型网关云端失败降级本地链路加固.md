---
name: "模型网关云端失败降级本地链路加固"
type: "fix"
tags: [gateway, model-watchdog, fallback, tests]
agent: "gateway-fallback-worker"
created: "2026-07-02T16:01:35.962697+00:00"
---

改了什么：补充 gateway retry 回归覆盖，钉住云端 5xx 重试耗尽后降级 llama、本地 llama.cpp 不可用再降级 ollama、以及本地全不可用返回 diagnostics 的行为；修复 gateway adapters import 排序使 ruff 通过。当前目标 diff 中还包含其他 agent 已落的 diagnostics、禁用 echo local、watchdog launch 配置等改动，我未回滚。

验证了什么：shell ruff check app/gateway app/services/model_watchdog tests/test_gateway_retry.py 通过；工具台 lint 对 backend/app/gateway、backend/app/services/model_watchdog、backend/tests/test_gateway_retry.py 通过；pytest tests/test_gateway_retry.py tests/test_gateway_protocol.py tests/test_gateway_tool_call_accumulator.py tests/test_gateway_adapters.py 为 42 passed；工具台 probe /api/gateway/models 返回 llama/ollama profiles，/api/gateway/health 返回 200 且显示 llama true、ollama/mimo false。

残留风险：未实际调用云端聊天或启动本地大模型，避免成本和副作用；真实降级依赖 LLAMA_CPP_SERVER_BIN/LOCAL_MODEL_ROOT 与本机模型文件存在。
