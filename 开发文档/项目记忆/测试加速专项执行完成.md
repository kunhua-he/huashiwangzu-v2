---
name: "测试加速专项执行完成"
type: "task"
tags: [testing, dev-toolkit, release-gate, playwright, sandbox-matrix, smoke]
agent: "codex-test-acceleration-conductor"
created: "2026-07-03T18:49:35.491426+00:00"
---

# 改了什么

- release gate 普通 pytest 改为真实 CLI preflight 路径，full `--skip-ui` gate 变为 `slow/integration` opt-in。
- release gate probe 增加 401 后强制刷新 token 并重试一次，避免并发 smoke/release gate 登录导致审计接口拿到失效 token。
- `module_sandbox_matrix.py` 增加 `--module` / `--modules` 定向过滤，以及显式 opt-in `--jobs` / `--frontend-jobs` 并发；默认仍串行兼容。
- `test_module_sandbox_matrix.py` 改为函数/fixture 复用扫描结果，只保留一条 CLI 单模块 smoke。
- UI e2e 手动截图默认关闭，`UI_E2E_SCREENSHOTS=1` 时才写截图；报告继续兼容空截图路径。
- smoke 增加 token cache/401 refresh、queue settle 快速探测和清理后跳过第二轮长等待。
- 补 `backend/pytest.ini` 和根 `pytest.ini` marker，更新 `dev_toolkit/README.md` 的 fast/targeted/integration/full 口径。

# 验证了什么

- `ruff` 目标文件通过。
- `test_release_gate.py`: 13 passed, 1 skipped, 2.07s pytest / 2.599s wall。
- `test_module_sandbox_matrix.py`: 16 passed, 0.08s pytest / 0.501s wall。
- `test_tool_job_tools.py`: 11 passed, 3.25s pytest / 3.670s wall。
- `test_smoke_queue_gate.py`: 5 passed, 0.01s pytest / 0.455s wall。
- `node --check frontend/tests/ui-e2e.spec.mjs` 通过；Playwright `--list` 列出 36 tests。
- `module_sandbox_matrix.py --json --module knowledge`: 单条 knowledge，0.053s wall。
- `module_sandbox_matrix.py --check --module knowledge`: backend/frontend PASS，3.077s wall。
- `release_gate.py --skip-ui --preflight`: PASS_WITH_DEBT，无 BLOCKER，1.953s wall。
- `SMOKE_SKIP_UI=1 smoke.py`: PASS_WITH_DEBT，28 pass / 0 fail / 1 skipped，33.141s wall。
- `git diff --check` 通过；worktree_guard 无越界/forbidden。

# 是否还有残留风险

- UI e2e 仍保持 serial，因为 `results`、`consoleCollector`、`fileIds`、`uploadedFilesById` 和 cleanup/report 聚合共享状态较多；强拆并行需另开专项隔离状态。
- `--jobs` 并发是显式 opt-in，未跑全量并发 matrix，避免并行开发时触发大量 sandbox/npm 构建扰动。

# 关联 commit

未提交。
