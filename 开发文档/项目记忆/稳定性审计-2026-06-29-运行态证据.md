---
name: "稳定性审计-2026-06-29-运行态证据"
type: task
tags: ["审计", "稳定性", "多worker", "任务队列", "事件总线", "knowledge", "agent", "测试门禁"]
created: 2026-06-29
agent: codex
---

本次只读审计，未修改产品代码、未提交。重点按局域网 20-50 人、workers=3 的底座稳定性排序。

确认问题：
1. task_worker 每个 worker 启动时都会把全部 running 任务当孤儿回收；数据库任务 2636 在约 4 秒内 retry_count 被推到 3 并以 Orphan task exceeded max retries 失败，证明多 worker 启动竞态。
2. knowledge stale_tracker 仍导入已不存在的 app.services.file_service.get_file_storage_path。数据库有 210 个 kb_pipeline 返回该 ImportError，但 task_worker 把返回 {error,status:failed} 当成功，状态写 completed；kb_documents 235 条中 232 条 parse_status=pending。
3. profile_evolve 的 conversation_service/router 有 4 处 from init_db import；数据库 128 个 profile_evolve 真失败，错误 No module named init_db。
4. event_bus 先把正常事件写 pending 再执行 handler，三个 worker 的 retry_failed_events 无行锁/claim，能重放仍在正常执行的事件并产生重复副作用；kb_documents 已存在 file_id=218 的两条并发重复记录 199/200（同一毫秒级窗口，因果需修复后专项复验）。
5. health endpoint 固定 status=ok/HTTP 200，不纳入 DB、worker、event bus、模块错误及任务失败积压；当前上述故障下仍显示绿。
6. Agent 的 budget/stuck/hook/recall JSON 均是无进程锁的 read-modify-temp-rename，只保证 JSON 不破损，不保证多 worker 不丢更新；test_multiworker_file_race 只断言可解析，未断言所有 writer 记录保留。
7. 验证门禁当前断裂：全量 pytest 收集因 test_agent_inline_tool_calls 导入失败中止；排除此文件后 407 passed/4 failed；前端 build 通过，但 Playwright global setup 仍找英文 Username/Password，实际 UI 为中文；dev_toolkit smoke 在当前代理环境因 httpx trust_env 解析 ::1 为 Invalid port ':1' 启动即崩；MCP lint/smoke_all 返回未知工具。
8. capability drift：desktop-tools 运行时注册 replace_file/refresh，但 manifest 未声明。
9. UI 真机复现：双击 Agent 对话标题重命名，ConversationSidebar 的 v-for template ref 实际为数组，nextTick 中 editInputRef.value?.focus() 报 focus is not a function。
10. 规则卫生：ruff check backend/app modules = 651 errors（主要 I001/F401/E712），前端 any/类型压制扫描 33 处。

验证：/api/health 200；frontend npm run build 通过；浏览器登录与桌面渲染正常、控制台在重命名交互出现上述 TypeError；工作区原有 5 个运行态数据文件 dirty，审计未新增产品 diff。
