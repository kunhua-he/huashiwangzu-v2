---
name: "稳定性审计-底座健康信号与异步队列风险"
type: task
tags: ["audit", "stability", "health", "task-worker", "excel-engine"]
created: 2026-07-02
agent: codex
---

# 审计范围
- 读取项目入口文档与框架/底层/模块开发文档。
- 通过 project_toolkit brief/routes/capabilities/db_schema/probe/smoke_all/run_test 与 codegraph/源码扫描审计当前底座稳定性。
- 本次未改代码，未提交 commit。

# 主要发现
1. /api/system/status 与 /api/health 的 worker 信号矛盾：system_status_service 仍按 background_worker 进程名扫描，当前实际是 app.main lifespan 内嵌 task worker，因此 /api/system/status 报 worker false，而 /api/health 报 worker.running true。
2. smoke_all(skip_ui=true) 全绿但异步队列表仍有噪音：/api/tasks/worker/status 显示 pending 1、failed 700+，近期 kb_pipeline File not found 多来自 smoke 上传后清理触发的异步管线失败，说明 smoke 覆盖不到异步失败与清理竞态。
3. excel-engine 多个 HTTP 端点把 legacy {'code':1,'msg':...} 包在 ApiResponse(data=result) 中返回，存在业务失败被 HTTP success 包住的 fake-green 风险。
4. 前端/模块存在多处 any/as any/@ts-ignore 风险，可能隐藏 API 字段漂移。
5. 当前工作区 dirty 面较大，含框架、agent、excel-engine、image-vision、文档与新增 IR 文件/测试，发布前应拆分收口并补回归。

# 已验证
- smoke_all(skip_ui=true)：26/26 green。
- backend/tests/test_content_ir_architecture.py：38 passed。
- backend/tests/test_assistant_draft.py：6 passed。
- dirty Python scope ruff check：All checks passed。

# 残留风险
- 未运行完整 UI Playwright/前端 build。
- 未修复发现项，仅完成审计定位。
