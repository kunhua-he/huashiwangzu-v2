---
name: "紧急维修08验收未通过-事件总线仍假绿"
type: task
tags: ["验收", "稳定性", "event_bus", "假绿", "多worker"]
created: 2026-06-29
agent: codex
---

验收 commit d2d7336。通过项：后端定向 9/9、全量 pytest 446/446、frontend build、Browser 实测 Agent 会话双击重命名获得焦点且控制台无错误。阻断项：真实 PostgreSQL 执行 event_bus._update_event_log 持续报 asyncpg AmbiguousParameterError（:status 同时用于 varchar SET 与 CASE 比较），导致本轮测试产生的 36 条 file.uploaded 事件卡 processing；/api/health 仅探测 get_event_log 可查询，仍返回 event_bus=ok/status=ok；全量测试污染当前库，新增约34条 kb_documents 与大量失败 kb_pipeline，违反测试数据清理规则；当前 MCP 会话真实 lint/smoke_all 仍返回未知工具，需重连后复验。另回信元数据写未提交，但实际已有 commit，且提交夹带运行态 JSON/历史记忆文件。结论：不通过，先修 event_bus 真库 SQL 与 health 健康判定、隔离/清理测试数据，再复验。
