---
name: "资产债务安全清偿与删除事件全覆盖二期完成"
type: "task"
tags: [asset-lifecycle, content-package, knowledge, test-pollution, release-gate, r2]
agent: "codex-asset-debt-cleanup-r2"
created: "2026-07-04T10:24:18.258193+00:00"
---

# 改了什么

执行资产债务安全清偿与删除事件全覆盖二期：补齐文件永久删除事件覆盖（单文件、文件夹子文件、empty_trash、desktop-tools 删除路径），清偿历史 Knowledge / ContentPackage / 测试污染债务，并将 release gate 资产项从 BLOCKER 收口为 PASS。

关键落点：`recycle_service.delete_permanently` / `empty_trash` 返回 `permanently_deleted_file_ids`；router 发 `file.permanent_deleted`；ContentPackage lifecycle 审计/归档/repair 能力注册；Knowledge 历史 source unavailable 归档；dev_toolkit test data pollution marker 收紧，去掉任意 `test-` 强 marker；release gate 输出 `release_safe` / `deploy_allowed` / `clean_release_ready` 语义稳定。

# 验证了什么

ruff target files pass。pytest：`test_content_package_lifecycle.py` 5 passed，`test_recycle_lifecycle_events.py` 5 passed，`test_desktop_tools_lifecycle.py` 1 passed，`test_content_ir_architecture.py` 55 passed，`modules/knowledge/sandbox/test_module.py` 15 passed，dev_toolkit asset/release tests 26 passed / 1 skipped。

活栈：`/api/health` ok；Knowledge lifecycle matched=0；ContentPackage source_unavailable=0、archived_by_lifecycle=207、missing_current_version_blocker=0；test_data_pollution_audit 全 0；错误 confirm 被拒绝，真实 confirm 在零候选状态幂等返回 changed=0；`release_gate(preflight, skip_ui=true)` 返回 PASS_WITH_DEBT，资产三项 PASS，`release_safe=true`、`deploy_allowed=true`、`clean_release_ready=false`。

# 是否还有残留风险

release gate 当前剩余 debt 是脏工作树、preflight/skip-ui 跳过 smoke/UI/sandbox，不是资产生命周期或测试污染 blocker。工作树仍有前端/agent 并行改动，本任务未回退未触碰。

# 关联 commit

未提交。
